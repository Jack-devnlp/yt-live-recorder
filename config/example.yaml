# YouTube Live Recorder Configuration Example
# Place your configuration in a file like config.yaml and run:
#   yt-recorder --monitor -c config.yaml

channels:
  - name: "Streamer1"
    channel_id: "UCxxxxxxxxxxxxxxxxxxx1"
  - name: "Streamer2"
    channel_id: "UCxxxxxxxxxxxxxxxxxxx2"
  # Add up to 5 channels
  # - name: "Your Favorite Streamer"
  #   channel_id: "UCxxxxxxxxxxxxxxxxxxx3"

settings:
  output_dir: "./recordings"
  quality: "best"  # Options: best, 1080p, 720p, 480p, 360p
